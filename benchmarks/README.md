# Text-only Assistant Benchmark

Run `python -m benchmarks list` at the repository root. The executable caller uses
Room Hub's actual core against fresh synthetic state. Gold answers stay outside
the runtime worker. No production rule, model, prompt or UI is changed.

[Architecture, modes, metrics, commands, limits and V35 workflow](../docs/ASSISTANT_BENCHMARK.md)

[Included V1 dataset and extension format](suites/room_hub_v1/README.md)
